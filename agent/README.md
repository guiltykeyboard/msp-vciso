# Endpoint collector

The endpoint collector is not implemented yet. The previous `app.py` file was a comment-only placeholder and has been removed.

The planned collector is a signed Go service, Windows-first, with tenant-bound enrollment, secure local credential storage, an allow-listed posture fact set, and no collection of files, screen contents, browser history, or CJI. Its protocol will be specified and threat-modeled before implementation.

